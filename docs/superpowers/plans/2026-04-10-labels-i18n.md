# Labels i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Centralise all hardcoded UI strings in a `labels` DB table, expose them via two public API endpoints, and wire a `useLabels()` hook across the frontend so the language can be switched at runtime from Settings without a redeploy.

**Architecture:** SQLite `labels(key, locale, value)` table seeded with EN+FR in the migration; two Express routes (no auth required); frontend reads labels once per locale into React Query cache with a static EN JSON fallback for offline/startup; `useLocale` persists the chosen locale in localStorage and triggers a cache invalidation on change.

**Tech Stack:** better-sqlite3, Express 5, Zod, Vitest (backend), React Query v5, Vitest + vi.mock (frontend), TypeScript.

---

## File Map

### New files
| Path | Responsibility |
|---|---|
| `backend/database/migrations/011_create_labels.sql` | Table DDL + full EN+FR seed |
| `backend/repositories/LabelsRepository.ts` | SQL: `getByLocale`, `getAvailableLocales` |
| `backend/services/LabelsService.ts` | Validates locale, delegates to repo |
| `backend/routes/labels.ts` | `GET /locales`, `GET /` with Zod query validation |
| `backend/__tests__/unit/repositories/LabelsRepository.test.ts` | Repo unit tests |
| `backend/__tests__/unit/services/LabelsService.test.ts` | Service unit tests |
| `backend/__tests__/routes/labels.routes.test.ts` | Route integration tests |
| `src/i18n/en.json` | Static EN bundle fallback |
| `src/services/api/labelsApi.ts` | `fetchLabels(locale)`, `fetchLocales()` |
| `src/hooks/useApiReachable.ts` | Pings `/api/health`, exposes `isReachable` + `triggerRetry` |
| `src/hooks/useLocale.ts` | localStorage read/write + React Query invalidation |
| `src/hooks/useLabels.ts` | `t(key, fallback?)` built on React Query |
| `src/hooks/useLocales.ts` | Available locales from API |
| `src/__tests__/hooks/useLabels.test.ts` | Hook tests |
| `src/__tests__/hooks/useLocales.test.ts` | Hook tests |
| `src/__tests__/hooks/useApiReachable.test.ts` | Hook tests |

### Modified files
| Path | Change |
|---|---|
| `backend/__tests__/helpers/buildTestApp.ts` | Wire `LabelsRepository`, `LabelsService`, `createLabelsRouter` |
| `backend/server.ts` | Register `/api/v1/labels` route (no auth) |
| `src/hooks/useSettingsPage.ts` | Replace `language` stub with `useLocale` |
| `src/views/SettingsPageView.tsx` | Dynamic locale select, offline grey-out + retry button |

---

## Task 1: Migration — `labels` table + full seed

**Files:**
- Create: `backend/database/migrations/011_create_labels.sql`

- [ ] **Step 1: Create the migration file**

```sql
-- backend/database/migrations/011_create_labels.sql
CREATE TABLE IF NOT EXISTS labels (
  key    TEXT NOT NULL,
  locale TEXT NOT NULL,
  value  TEXT NOT NULL,
  PRIMARY KEY (key, locale)
);
CREATE INDEX IF NOT EXISTS idx_labels_locale ON labels(locale);

-- locale names (used by GET /api/v1/labels/locales)
INSERT INTO labels (key, locale, value) VALUES
  ('locale.en.name', 'en', 'English'),
  ('locale.en.name', 'fr', 'English'),
  ('locale.fr.name', 'en', 'Français'),
  ('locale.fr.name', 'fr', 'Français'),

  -- common
  ('common.buttons.save',    'en', 'Save'),
  ('common.buttons.save',    'fr', 'Enregistrer'),
  ('common.buttons.cancel',  'en', 'Cancel'),
  ('common.buttons.cancel',  'fr', 'Annuler'),
  ('common.buttons.delete',  'en', 'Delete'),
  ('common.buttons.delete',  'fr', 'Supprimer'),
  ('common.buttons.edit',    'en', 'Edit'),
  ('common.buttons.edit',    'fr', 'Modifier'),
  ('common.buttons.add',     'en', 'Add'),
  ('common.buttons.add',     'fr', 'Ajouter'),
  ('common.buttons.confirm', 'en', 'Confirm'),
  ('common.buttons.confirm', 'fr', 'Confirmer'),
  ('common.buttons.back',    'en', 'Back'),
  ('common.buttons.back',    'fr', 'Retour'),
  ('common.buttons.retry',   'en', 'Retry'),
  ('common.buttons.retry',   'fr', 'Réessayer'),
  ('common.buttons.submit',  'en', 'Submit'),
  ('common.buttons.submit',  'fr', 'Valider'),
  ('common.buttons.create',  'en', 'Create'),
  ('common.buttons.create',  'fr', 'Créer'),
  ('common.buttons.close',   'en', 'Close'),
  ('common.buttons.close',   'fr', 'Fermer'),
  ('common.buttons.loading', 'en', 'Loading...'),
  ('common.buttons.loading', 'fr', 'Chargement...'),
  ('common.search.placeholder', 'en', 'Search...'),
  ('common.search.placeholder', 'fr', 'Rechercher...'),
  ('common.empty.no_results',   'en', 'No results found'),
  ('common.empty.no_results',   'fr', 'Aucun résultat'),
  ('common.action.cannot_undo', 'en', 'This action cannot be undone.'),
  ('common.action.cannot_undo', 'fr', 'Cette action est irréversible.'),

  -- auth
  ('auth.title',                'en', 'Board Game Dashboard'),
  ('auth.title',                'fr', 'Board Game Dashboard'),
  ('auth.subtitle',             'en', 'Enter your password to continue'),
  ('auth.subtitle',             'fr', 'Entrez votre mot de passe pour continuer'),
  ('auth.password.label',       'en', 'Password'),
  ('auth.password.label',       'fr', 'Mot de passe'),
  ('auth.submit',               'en', 'Sign in'),
  ('auth.submit',               'fr', 'Se connecter'),
  ('auth.submit.loading',       'en', 'Signing in...'),
  ('auth.submit.loading',       'fr', 'Connexion...'),
  ('auth.error.wrong_password', 'en', 'Incorrect password. Please try again.'),
  ('auth.error.wrong_password', 'fr', 'Mot de passe incorrect. Veuillez réessayer.'),

  -- dashboard
  ('dashboard.title',                   'en', 'Dashboard'),
  ('dashboard.title',                   'fr', 'Dashboard'),
  ('dashboard.tooltip.back',            'en', 'Go Back'),
  ('dashboard.tooltip.back',            'fr', 'Retour'),
  ('dashboard.tooltip.settings',        'en', 'Settings'),
  ('dashboard.tooltip.settings',        'fr', 'Paramètres'),
  ('dashboard.section.players',         'en', 'Player Statistics'),
  ('dashboard.section.players',         'fr', 'Statistiques joueurs'),
  ('dashboard.section.games',           'en', 'Recent Games'),
  ('dashboard.section.games',           'fr', 'Jeux récents'),
  ('dashboard.section.activity',        'en', 'Recent Activity'),
  ('dashboard.section.activity',        'fr', 'Activité récente'),
  ('dashboard.players.empty',           'en', 'No players yet'),
  ('dashboard.players.empty',           'fr', 'Aucun joueur'),
  ('dashboard.players.add_first',       'en', 'Add your first player'),
  ('dashboard.players.add_first',       'fr', 'Ajouter votre premier joueur'),
  ('dashboard.games.empty',             'en', 'No games yet'),
  ('dashboard.games.empty',             'fr', 'Aucun jeu'),
  ('dashboard.games.add_first',         'en', 'Add your first game'),
  ('dashboard.games.add_first',         'fr', 'Ajouter votre premier jeu'),
  ('dashboard.activity.game_started',   'en', 'New game started'),
  ('dashboard.activity.game_started',   'fr', 'Nouvelle partie démarrée'),
  ('dashboard.activity.player_joined',  'en', 'Player joined'),
  ('dashboard.activity.player_joined',  'fr', 'Joueur rejoint'),
  ('dashboard.actions.new_game',        'en', 'New Game'),
  ('dashboard.actions.new_game',        'fr', 'Nouvelle partie'),
  ('dashboard.actions.new_game.tooltip','en', 'Start a New Game Session'),
  ('dashboard.actions.new_game.tooltip','fr', 'Démarrer une nouvelle partie'),
  ('dashboard.actions.add_player',        'en', 'Add Player'),
  ('dashboard.actions.add_player',        'fr', 'Ajouter joueur'),
  ('dashboard.actions.add_player.tooltip','en', 'Go to Players Page'),
  ('dashboard.actions.add_player.tooltip','fr', 'Aller à la page joueurs'),
  ('dashboard.tooltip.view_players',    'en', 'View All Players'),
  ('dashboard.tooltip.view_players',    'fr', 'Voir tous les joueurs'),
  ('dashboard.tooltip.view_games',      'en', 'View All Games'),
  ('dashboard.tooltip.view_games',      'fr', 'Voir tous les jeux'),

  -- games page
  ('games.page.title',              'en', 'Games'),
  ('games.page.title',              'fr', 'Jeux'),
  ('games.stats.total',             'en', 'Total Games'),
  ('games.stats.total',             'fr', 'Total jeux'),
  ('games.stats.categories',        'en', 'Categories'),
  ('games.stats.categories',        'fr', 'Catégories'),
  ('games.stats.avg_rating',        'en', 'Avg Rating'),
  ('games.stats.avg_rating',        'fr', 'Note moyenne'),
  ('games.search.placeholder',      'en', 'Search games, designers, publishers...'),
  ('games.search.placeholder',      'fr', 'Rechercher jeux, créateurs, éditeurs...'),
  ('games.card.players',            'en', 'players'),
  ('games.card.players',            'fr', 'joueurs'),
  ('games.card.weight',             'en', 'Weight:'),
  ('games.card.weight',             'fr', 'Poids :'),
  ('games.card.expansion',          'en', 'Extension'),
  ('games.card.expansion',          'fr', 'Extension'),
  ('games.card.expansion.count',    'en', 'expansion'),
  ('games.card.expansion.count',    'fr', 'extension'),
  ('games.card.expansion.count_plural', 'en', 'expansions'),
  ('games.card.expansion.count_plural', 'fr', 'extensions'),
  ('games.card.character.count',        'en', 'character'),
  ('games.card.character.count',        'fr', 'personnage'),
  ('games.card.character.count_plural', 'en', 'characters'),
  ('games.card.character.count_plural', 'fr', 'personnages'),
  ('games.card.section.expansions',     'en', 'Expansions'),
  ('games.card.section.expansions',     'fr', 'Extensions'),
  ('games.card.section.characters',     'en', 'Characters/Roles'),
  ('games.card.section.characters',     'fr', 'Personnages/Rôles'),
  ('games.card.modes.competitive',  'en', 'Compétitif'),
  ('games.card.modes.competitive',  'fr', 'Compétitif'),
  ('games.card.modes.cooperative',  'en', 'Coopératif'),
  ('games.card.modes.cooperative',  'fr', 'Coopératif'),
  ('games.card.modes.campaign',     'en', 'Campagne'),
  ('games.card.modes.campaign',     'fr', 'Campagne'),
  ('games.card.modes.hybrid',       'en', 'Hybride'),
  ('games.card.modes.hybrid',       'fr', 'Hybride'),
  ('games.empty',                   'en', 'No games found'),
  ('games.empty',                   'fr', 'Aucun jeu trouvé'),
  ('games.tooltip.view_details',    'en', 'View Details'),
  ('games.tooltip.view_details',    'fr', 'Voir les détails'),
  ('games.tooltip.view_stats',      'en', 'View Game Stats'),
  ('games.tooltip.view_stats',      'fr', 'Voir les stats'),
  ('games.tooltip.edit',            'en', 'Edit Game'),
  ('games.tooltip.edit',            'fr', 'Modifier le jeu'),
  ('games.menu.view_details',       'en', 'View Details'),
  ('games.menu.view_details',       'fr', 'Voir les détails'),
  ('games.menu.view_stats',         'en', 'View Stats'),
  ('games.menu.view_stats',         'fr', 'Voir les stats'),
  ('games.menu.edit',               'en', 'Edit Game'),
  ('games.menu.edit',               'fr', 'Modifier le jeu'),
  ('games.menu.expansions',         'en', 'Manage Expansions'),
  ('games.menu.expansions',         'fr', 'Gérer les extensions'),
  ('games.menu.characters',         'en', 'Manage Characters'),
  ('games.menu.characters',         'fr', 'Gérer les personnages'),
  ('games.menu.delete',             'en', 'Delete Game'),
  ('games.menu.delete',             'fr', 'Supprimer le jeu'),
  ('games.add_dialog.title',        'en', 'Add New Game'),
  ('games.add_dialog.title',        'fr', 'Ajouter un nouveau jeu'),
  ('games.add_dialog.submit',       'en', 'Add Game'),
  ('games.add_dialog.submit',       'fr', 'Ajouter le jeu'),
  ('games.edit_dialog.title',       'en', 'Edit Game'),
  ('games.edit_dialog.title',       'fr', 'Modifier le jeu'),
  ('games.edit_dialog.submit',      'en', 'Update Game'),
  ('games.edit_dialog.submit',      'fr', 'Mettre à jour'),
  ('games.delete_dialog.title',     'en', 'Delete Game'),
  ('games.delete_dialog.title',     'fr', 'Supprimer le jeu'),
  ('games.delete_dialog.description','en','Are you sure you want to delete this game? This action cannot be undone and will also remove all associated expansions and characters.'),
  ('games.delete_dialog.description','fr','Supprimer ce jeu ? Cette action est irréversible et supprimera également toutes les extensions et personnages associés.'),
  ('games.form.name.label',              'en', 'Game Name'),
  ('games.form.name.label',              'fr', 'Nom du jeu'),
  ('games.form.name.placeholder',        'en', 'Enter game name'),
  ('games.form.name.placeholder',        'fr', 'Entrer le nom du jeu'),
  ('games.form.designer.label',          'en', 'Designer'),
  ('games.form.designer.label',          'fr', 'Créateur'),
  ('games.form.publisher.label',         'en', 'Publisher'),
  ('games.form.publisher.label',         'fr', 'Éditeur'),
  ('games.form.year.label',              'en', 'Year'),
  ('games.form.year.label',              'fr', 'Année'),
  ('games.form.bgg_rating.label',        'en', 'BGG Rating'),
  ('games.form.bgg_rating.label',        'fr', 'Note BGG'),
  ('games.form.description.label',       'en', 'Description'),
  ('games.form.description.label',       'fr', 'Description'),
  ('games.form.description.placeholder', 'en', 'Brief game description'),
  ('games.form.description.placeholder', 'fr', 'Brève description du jeu'),
  ('games.form.category.label',          'en', 'Category'),
  ('games.form.category.label',          'fr', 'Catégorie'),
  ('games.form.modes.label',             'en', 'Game Modes'),
  ('games.form.modes.label',             'fr', 'Modes de jeu'),
  ('games.form.is_expansion.label',      'en', 'Is an expansion'),
  ('games.form.is_expansion.label',      'fr', 'Est une extension'),
  ('games.form.has_expansion.label',     'en', 'Has expansions'),
  ('games.form.has_expansion.label',     'fr', 'A des extensions'),
  ('games.form.expansions.label',        'en', 'Expansions'),
  ('games.form.expansions.label',        'fr', 'Extensions'),
  ('games.form.has_characters.label',    'en', 'Has character roles'),
  ('games.form.has_characters.label',    'fr', 'A des rôles de personnages'),
  ('games.form.characters.section',      'en', 'Characters'),
  ('games.form.characters.section',      'fr', 'Personnages'),
  ('games.form.characters.name.placeholder','en', 'Character name'),
  ('games.form.characters.name.placeholder','fr', 'Nom du personnage'),
  ('games.form.characters.abilities.label','en', 'Abilities'),
  ('games.form.characters.abilities.label','fr', 'Capacités'),
  ('games.form.characters.ability.placeholder','en', 'Ability name'),
  ('games.form.characters.ability.placeholder','fr', 'Nom de la capacité'),

  -- players page
  ('players.page.title',            'en', 'Players'),
  ('players.page.title',            'fr', 'Joueurs'),
  ('players.stats.total',           'en', 'Players'),
  ('players.stats.total',           'fr', 'Joueurs'),
  ('players.stats.games',           'en', 'Games'),
  ('players.stats.games',           'fr', 'Parties'),
  ('players.stats.wins',            'en', 'Wins'),
  ('players.stats.wins',            'fr', 'Victoires'),
  ('players.search.placeholder',    'en', 'Search players...'),
  ('players.search.placeholder',    'fr', 'Rechercher des joueurs...'),
  ('players.card.games',            'en', 'games'),
  ('players.card.games',            'fr', 'parties'),
  ('players.card.wins',             'en', 'wins'),
  ('players.card.wins',             'fr', 'victoires'),
  ('players.card.avg',              'en', 'Avg:'),
  ('players.card.avg',              'fr', 'Moy :'),
  ('players.card.menu.view_stats',  'en', 'View Stats'),
  ('players.card.menu.view_stats',  'fr', 'Voir les stats'),
  ('players.card.menu.edit',        'en', 'Edit'),
  ('players.card.menu.edit',        'fr', 'Modifier'),
  ('players.card.menu.delete',      'en', 'Delete'),
  ('players.card.menu.delete',      'fr', 'Supprimer'),
  ('players.empty',                 'en', 'No players found'),
  ('players.empty',                 'fr', 'Aucun joueur trouvé'),
  ('players.empty.add_first',       'en', 'Add Your First Player'),
  ('players.empty.add_first',       'fr', 'Ajouter votre premier joueur'),
  ('players.add_dialog.title',      'en', 'Add New Player'),
  ('players.add_dialog.title',      'fr', 'Ajouter un joueur'),
  ('players.edit_dialog.title',     'en', 'Edit Player'),
  ('players.edit_dialog.title',     'fr', 'Modifier le joueur'),
  ('players.delete_dialog.title',   'en', 'Delete Player'),
  ('players.delete_dialog.title',   'fr', 'Supprimer le joueur'),
  ('players.delete_dialog.description','en','Are you sure you want to delete this player? This action cannot be undone.'),
  ('players.delete_dialog.description','fr','Supprimer ce joueur ? Cette action est irréversible.'),
  ('players.form.name.label',            'en', 'Name'),
  ('players.form.name.label',            'fr', 'Nom'),
  ('players.form.avatar.label',          'en', 'Avatar URL'),
  ('players.form.avatar.label',          'fr', 'URL de l''avatar'),
  ('players.form.favorite_game.label',   'en', 'Favorite Game'),
  ('players.form.favorite_game.label',   'fr', 'Jeu favori'),
  ('players.form.favorite_game.placeholder','en', 'Enter favorite game (optional)'),
  ('players.form.favorite_game.placeholder','fr', 'Jeu favori (optionnel)'),

  -- sessions
  ('sessions.new.title',                     'en', 'New Game Session'),
  ('sessions.new.title',                     'fr', 'Nouvelle partie'),
  ('sessions.setup.title',                   'en', 'Game Setup'),
  ('sessions.setup.title',                   'fr', 'Configuration'),
  ('sessions.setup.game.label',              'en', 'Game'),
  ('sessions.setup.game.label',              'fr', 'Jeu'),
  ('sessions.setup.game.placeholder',        'en', 'Choose a game...'),
  ('sessions.setup.game.placeholder',        'fr', 'Choisir un jeu...'),
  ('sessions.setup.type.label',              'en', 'Session Type'),
  ('sessions.setup.type.label',              'fr', 'Type de session'),
  ('sessions.setup.type.competitive',        'en', 'Competitive - Players compete against each other'),
  ('sessions.setup.type.competitive',        'fr', 'Compétitif - Les joueurs s''affrontent'),
  ('sessions.setup.type.cooperative',        'en', 'Cooperative - Players work together'),
  ('sessions.setup.type.cooperative',        'fr', 'Coopératif - Les joueurs coopèrent'),
  ('sessions.setup.type.campaign',           'en', 'Campaign - Ongoing story mode'),
  ('sessions.setup.type.campaign',           'fr', 'Campagne - Mode histoire'),
  ('sessions.setup.type.hybrid',             'en', 'Hybrid - Mix of team and individual goals'),
  ('sessions.setup.type.hybrid',             'fr', 'Hybride - Mélange d''objectifs équipe et individuels'),
  ('sessions.players.title',                 'en', 'Select Players'),
  ('sessions.players.title',                 'fr', 'Sélectionner les joueurs'),
  ('sessions.cooperative.title',             'en', 'Cooperative Objectives'),
  ('sessions.cooperative.title',             'fr', 'Objectifs coopératifs'),
  ('sessions.cooperative.difficulty.label',  'en', 'Difficulty Level'),
  ('sessions.cooperative.difficulty.label',  'fr', 'Niveau de difficulté'),
  ('sessions.cooperative.team_success',      'en', 'Team Victory'),
  ('sessions.cooperative.team_success',      'fr', 'Victoire d''équipe'),
  ('sessions.cooperative.team_score.label',  'en', 'Team Score'),
  ('sessions.cooperative.team_score.label',  'fr', 'Score d''équipe'),
  ('sessions.cooperative.objectives.label',  'en', 'Shared Objectives'),
  ('sessions.cooperative.objectives.label',  'fr', 'Objectifs partagés'),
  ('sessions.cooperative.objectives.add_common', 'en', 'Add Common'),
  ('sessions.cooperative.objectives.add_common', 'fr', 'Ajouter communs'),
  ('sessions.cooperative.objectives.add_custom', 'en', 'Add Custom'),
  ('sessions.cooperative.objectives.add_custom', 'fr', 'Ajouter personnalisé'),
  ('sessions.cooperative.objectives.empty',      'en', 'No objectives set yet'),
  ('sessions.cooperative.objectives.empty',      'fr', 'Aucun objectif défini'),
  ('sessions.cooperative.objectives.completed',  'en', 'Completed'),
  ('sessions.cooperative.objectives.completed',  'fr', 'Accompli'),
  ('sessions.competitive.title',             'en', 'Competitive Scoring'),
  ('sessions.competitive.title',             'fr', 'Scores compétitifs'),
  ('sessions.competitive.winner_missing',    'en', 'A winner must be designated'),
  ('sessions.competitive.winner_missing',    'fr', 'Un vainqueur doit être désigné'),
  ('sessions.competitive.score_invalid',     'en', 'The winner''s score must be greater than 0'),
  ('sessions.competitive.score_invalid',     'fr', 'Le score du vainqueur doit être supérieur à 0'),
  ('sessions.competitive.winner',            'en', 'Winner'),
  ('sessions.competitive.winner',            'fr', 'Vainqueur'),
  ('sessions.hybrid.team.title',             'en', 'Team Objectives'),
  ('sessions.hybrid.team.title',             'fr', 'Objectifs d''équipe'),
  ('sessions.hybrid.team.completed',         'en', 'Team Objectives Completed'),
  ('sessions.hybrid.team.completed',         'fr', 'Objectifs d''équipe accomplis'),
  ('sessions.hybrid.team.score',             'en', 'Team Score'),
  ('sessions.hybrid.team.score',             'fr', 'Score d''équipe'),
  ('sessions.hybrid.individual.title',       'en', 'Individual Scores'),
  ('sessions.hybrid.individual.title',       'fr', 'Scores individuels'),
  ('sessions.details.title',                 'en', 'Session Details'),
  ('sessions.details.title',                 'fr', 'Détails de la session'),
  ('sessions.details.duration.label',        'en', 'Duration (minutes)'),
  ('sessions.details.duration.label',        'fr', 'Durée (minutes)'),
  ('sessions.details.duration.required',     'en', 'Required'),
  ('sessions.details.duration.required',     'fr', 'Obligatoire'),
  ('sessions.details.notes.label',           'en', 'Notes'),
  ('sessions.details.notes.label',           'fr', 'Notes'),
  ('sessions.details.notes.placeholder',     'en', 'Game notes, highlights, or observations...'),
  ('sessions.details.notes.placeholder',     'fr', 'Notes, moments forts ou observations...'),
  ('sessions.submit',                        'en', 'Create Session'),
  ('sessions.submit',                        'fr', 'Créer la session'),
  ('sessions.submit.loading',                'en', 'Creating...'),
  ('sessions.submit.loading',                'fr', 'Création...'),

  -- stats game
  ('stats.game.no_data',            'en', 'No game data available'),
  ('stats.game.no_data',            'fr', 'Aucune donnée disponible'),
  ('stats.game.all_title',          'en', 'All Games Statistics'),
  ('stats.game.all_title',          'fr', 'Statistiques tous jeux'),
  ('stats.game.stat.sessions',      'en', 'Sessions'),
  ('stats.game.stat.sessions',      'fr', 'Sessions'),
  ('stats.game.stat.avg_players',   'en', 'Avg Players'),
  ('stats.game.stat.avg_players',   'fr', 'Joueurs moy.'),
  ('stats.game.stat.avg_duration',  'en', 'Avg Duration'),
  ('stats.game.stat.avg_duration',  'fr', 'Durée moy.'),
  ('stats.game.stat.avg_score',     'en', 'Avg Score'),
  ('stats.game.stat.avg_score',     'fr', 'Score moy.'),
  ('stats.game.back_to_all',        'en', '← Back to All Games Statistics'),
  ('stats.game.back_to_all',        'fr', '← Retour aux statistiques globales'),
  ('stats.game.select.title',       'en', 'Select a Game for Detailed Stats'),
  ('stats.game.select.title',       'fr', 'Choisir un jeu pour les stats détaillées'),
  ('stats.game.popularity.title',   'en', 'Game Popularity'),
  ('stats.game.popularity.title',   'fr', 'Popularité des jeux'),
  ('stats.game.sessions_label',     'en', 'sessions'),
  ('stats.game.sessions_label',     'fr', 'sessions'),
  ('stats.game.score_trend.title',  'en', 'Score Trend'),
  ('stats.game.score_trend.title',  'fr', 'Évolution des scores'),
  ('stats.game.score_trend.subtitle','en', 'Last 10 Sessions'),
  ('stats.game.score_trend.subtitle','fr', '10 dernières sessions'),
  ('stats.game.session_types.title','en', 'Session Types'),
  ('stats.game.session_types.title','fr', 'Types de sessions'),
  ('stats.game.player_dist.title',  'en', 'Player Count Distribution'),
  ('stats.game.player_dist.title',  'fr', 'Distribution nb joueurs'),
  ('stats.game.player_dist.label',  'en', 'players'),
  ('stats.game.player_dist.label',  'fr', 'joueurs'),
  ('stats.game.top_winners.title',  'en', 'Top Winners'),
  ('stats.game.top_winners.title',  'fr', 'Meilleurs joueurs'),
  ('stats.game.top_winners.wins',   'en', 'wins'),
  ('stats.game.top_winners.wins',   'fr', 'victoires'),
  ('stats.game.recent.title',       'en', 'Recent Sessions'),
  ('stats.game.recent.title',       'fr', 'Sessions récentes'),
  ('stats.game.recent.winner',      'en', 'Winner:'),
  ('stats.game.recent.winner',      'fr', 'Vainqueur :'),
  ('stats.game.recent.avg_score',   'en', 'avg score'),
  ('stats.game.recent.avg_score',   'fr', 'score moy.'),

  -- stats player
  ('stats.player.profile',            'en', 'Player Profile'),
  ('stats.player.profile',            'fr', 'Profil joueur'),
  ('stats.player.stat.wins',          'en', 'Wins'),
  ('stats.player.stat.wins',          'fr', 'Victoires'),
  ('stats.player.stat.games_played',  'en', 'Games Played'),
  ('stats.player.stat.games_played',  'fr', 'Parties jouées'),
  ('stats.player.stat.total_score',   'en', 'Total Score'),
  ('stats.player.stat.total_score',   'fr', 'Score total'),
  ('stats.player.stat.avg_score',     'en', 'Avg Score'),
  ('stats.player.stat.avg_score',     'fr', 'Score moyen'),
  ('stats.player.stat.total_players', 'en', 'Total Players'),
  ('stats.player.stat.total_players', 'fr', 'Total joueurs'),
  ('stats.player.favorite_game',      'en', 'Favorite Game'),
  ('stats.player.favorite_game',      'fr', 'Jeu favori'),
  ('stats.player.top_players',        'en', 'Top Players'),
  ('stats.player.top_players',        'fr', 'Meilleurs joueurs'),
  ('stats.player.recent.empty',       'en', 'No recent games found'),
  ('stats.player.recent.empty',       'fr', 'Aucune partie récente'),
  ('stats.player.recent.winner',      'en', 'Winner'),
  ('stats.player.recent.winner',      'fr', 'Vainqueur'),
  ('stats.player.activity.title',     'en', 'Recent Activity'),
  ('stats.player.activity.title',     'fr', 'Activité récente'),
  ('stats.player.performance.coming_soon','en', 'Detailed charts coming soon...'),
  ('stats.player.performance.coming_soon','fr', 'Graphiques détaillés à venir...'),

  -- settings
  ('settings.page.title',            'en', 'Settings'),
  ('settings.page.title',            'fr', 'Paramètres'),
  ('settings.section.preferences',   'en', 'Preferences'),
  ('settings.section.preferences',   'fr', 'Préférences'),
  ('settings.notifications.label',   'en', 'Notifications'),
  ('settings.notifications.label',   'fr', 'Notifications'),
  ('settings.notifications.desc',    'en', 'Get notified about game updates'),
  ('settings.notifications.desc',    'fr', 'Recevoir les mises à jour'),
  ('settings.dark_mode.label',       'en', 'Dark Mode'),
  ('settings.dark_mode.label',       'fr', 'Mode sombre'),
  ('settings.dark_mode.desc',        'en', 'Use dark theme'),
  ('settings.dark_mode.desc',        'fr', 'Utiliser le thème sombre'),
  ('settings.language.label',        'en', 'Language'),
  ('settings.language.label',        'fr', 'Langue'),
  ('settings.language.desc',         'en', 'Choose your language'),
  ('settings.language.desc',         'fr', 'Choisir votre langue'),
  ('settings.language.offline',      'en', 'Unavailable offline'),
  ('settings.language.offline',      'fr', 'Indisponible hors ligne'),
  ('settings.language.retry',        'en', 'Retry'),
  ('settings.language.retry',        'fr', 'Réessayer'),
  ('settings.auto_save.label',       'en', 'Auto Save'),
  ('settings.auto_save.label',       'fr', 'Sauvegarde auto'),
  ('settings.auto_save.desc',        'en', 'Automatically save changes'),
  ('settings.auto_save.desc',        'fr', 'Enregistrer automatiquement'),
  ('settings.tooltips.label',        'en', 'Show Tooltips'),
  ('settings.tooltips.label',        'fr', 'Afficher les infobulles'),
  ('settings.tooltips.desc',         'en', 'Display helpful tooltips'),
  ('settings.tooltips.desc',         'fr', 'Afficher les infobulles'),
  ('settings.section.data',          'en', 'Data Management'),
  ('settings.section.data',          'fr', 'Gestion des données'),
  ('settings.data.bgg_imported',     'en', 'BGG Catalog imported'),
  ('settings.data.bgg_imported',     'fr', 'Catalogue BGG importé'),
  ('settings.data.export',           'en', 'Export Data'),
  ('settings.data.export',           'fr', 'Exporter les données'),
  ('settings.data.import',           'en', 'Import Data'),
  ('settings.data.import',           'fr', 'Importer les données'),
  ('settings.data.reset',            'en', 'Reset All Data'),
  ('settings.data.reset',            'fr', 'Réinitialiser toutes les données'),
  ('settings.data.bgg_catalog',      'en', 'BGG Catalog'),
  ('settings.data.bgg_catalog',      'fr', 'Catalogue BGG'),
  ('settings.data.bgg_not_imported', 'en', 'Not imported'),
  ('settings.data.bgg_not_imported', 'fr', 'Non importé'),
  ('settings.data.bgg_importing',    'en', 'Import in progress…'),
  ('settings.data.bgg_importing',    'fr', 'Import en cours…'),
  ('settings.data.bgg_import_file',  'en', 'Import boardgames_ranks.csv'),
  ('settings.data.bgg_import_file',  'fr', 'Importer boardgames_ranks.csv'),
  ('settings.section.about',         'en', 'About'),
  ('settings.section.about',         'fr', 'À propos'),
  ('settings.about.version',         'en', 'Board Game Dashboard v1.0.0'),
  ('settings.about.version',         'fr', 'Board Game Dashboard v1.0.0'),
  ('settings.about.desc',            'en', 'A modern dashboard for tracking your board game sessions and player statistics.'),
  ('settings.about.desc',            'fr', 'Un tableau de bord moderne pour suivre vos sessions et statistiques.'),
  ('settings.section.session',       'en', 'Session'),
  ('settings.section.session',       'fr', 'Session'),
  ('settings.logout',                'en', 'Sign out'),
  ('settings.logout',                'fr', 'Se déconnecter'),

  -- errors
  ('errors.generic',               'en', 'An error occurred'),
  ('errors.generic',               'fr', 'Une erreur est survenue'),
  ('errors.network',               'en', 'Network error. Please check your connection.'),
  ('errors.network',               'fr', 'Erreur réseau. Vérifiez votre connexion.'),
  ('errors.game.not_found',        'en', 'Game not found'),
  ('errors.game.not_found',        'fr', 'Jeu introuvable'),
  ('errors.game.duplicate',        'en', 'This game is already in your collection'),
  ('errors.game.duplicate',        'fr', 'Ce jeu est déjà dans votre collection'),
  ('errors.player.not_found',      'en', 'Player not found'),
  ('errors.player.not_found',      'fr', 'Joueur introuvable'),
  ('errors.player.duplicate_pseudo','en','This pseudo is already taken'),
  ('errors.player.duplicate_pseudo','fr','Ce pseudo est déjà utilisé'),
  ('errors.session.not_found',     'en', 'Session not found'),
  ('errors.session.not_found',     'fr', 'Session introuvable'),
  ('errors.auth.unauthorized',     'en', 'Unauthorized'),
  ('errors.auth.unauthorized',     'fr', 'Non autorisé'),
  ('errors.auth.wrong_password',   'en', 'Incorrect password'),
  ('errors.auth.wrong_password',   'fr', 'Mot de passe incorrect');
```

- [ ] **Step 2: Verify migration runs on a fresh DB**

```bash
cd backend && npm run init-db
```

Expected: no errors, DB initialises cleanly with migration 011 applied.

- [ ] **Step 3: Commit**

```bash
git checkout -b feat/labels-i18n
git add backend/database/migrations/011_create_labels.sql
git commit -m "feat(db): add labels table with EN+FR seed (migration 011)"
```

---

## Task 2: LabelsRepository

**Files:**
- Create: `backend/repositories/LabelsRepository.ts`
- Create: `backend/__tests__/unit/repositories/LabelsRepository.test.ts`

- [ ] **Step 1: Write the failing tests**

```typescript
// backend/__tests__/unit/repositories/LabelsRepository.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import { DatabaseConnection } from '../../../database/DatabaseConnection'
import { LabelsRepository } from '../../../repositories/LabelsRepository'

let conn: DatabaseConnection
let repo: LabelsRepository

beforeEach(() => {
  conn = new DatabaseConnection(':memory:')
  repo = new LabelsRepository(conn.db)
})

afterEach(() => conn.close())

describe('LabelsRepository', () => {
  it('getByLocale returns a flat key→value map for the given locale', () => {
    const labels = repo.getByLocale('en')
    expect(typeof labels).toBe('object')
    expect(labels['common.buttons.save']).toBe('Save')
    expect(labels['games.page.title']).toBe('Games')
    expect(labels['errors.generic']).toBe('An error occurred')
  })

  it('getByLocale returns FR values when locale is fr', () => {
    const labels = repo.getByLocale('fr')
    expect(labels['common.buttons.save']).toBe('Enregistrer')
    expect(labels['games.page.title']).toBe('Jeux')
  })

  it('getByLocale returns empty object for unknown locale', () => {
    const labels = repo.getByLocale('de')
    expect(Object.keys(labels)).toHaveLength(0)
  })

  it('getAvailableLocales returns en and fr', () => {
    const locales = repo.getAvailableLocales()
    expect(locales).toHaveLength(2)
    const codes = locales.map(l => l.locale)
    expect(codes).toContain('en')
    expect(codes).toContain('fr')
  })

  it('getAvailableLocales each entry has locale and name', () => {
    const locales = repo.getAvailableLocales()
    const en = locales.find(l => l.locale === 'en')
    expect(en?.name).toBe('English')
    const fr = locales.find(l => l.locale === 'fr')
    expect(fr?.name).toBe('Français')
  })
})
```

- [ ] **Step 2: Run to verify failure**

```bash
cd backend && npm run test:run -- __tests__/unit/repositories/LabelsRepository.test.ts
```

Expected: `Cannot find module '../../../repositories/LabelsRepository'`

- [ ] **Step 3: Implement LabelsRepository**

```typescript
// backend/repositories/LabelsRepository.ts
import type Database from 'better-sqlite3'

export interface LocaleInfo {
  locale: string
  name: string
}

export class LabelsRepository {
  constructor(private db: Database.Database) {}

  getByLocale(locale: string): Record<string, string> {
    const rows = this.db
      .prepare('SELECT key, value FROM labels WHERE locale = ?')
      .all(locale) as { key: string; value: string }[]
    return Object.fromEntries(rows.map(r => [r.key, r.value]))
  }

  getAvailableLocales(): LocaleInfo[] {
    const rows = this.db
      .prepare(`
        SELECT DISTINCT l.locale,
               COALESCE(n.value, l.locale) AS name
        FROM labels l
        LEFT JOIN labels n
          ON n.key = 'locale.' || l.locale || '.name'
         AND n.locale = l.locale
        WHERE l.locale != ''
        GROUP BY l.locale
        ORDER BY l.locale
      `)
      .all() as { locale: string; name: string }[]
    return rows
  }
}
```

- [ ] **Step 4: Run to verify tests pass**

```bash
cd backend && npm run test:run -- __tests__/unit/repositories/LabelsRepository.test.ts
```

Expected: 5 passing

- [ ] **Step 5: Commit**

```bash
git add backend/repositories/LabelsRepository.ts backend/__tests__/unit/repositories/LabelsRepository.test.ts
git commit -m "feat(backend): LabelsRepository — getByLocale + getAvailableLocales"
```

---

## Task 3: LabelsService

**Files:**
- Create: `backend/services/LabelsService.ts`
- Create: `backend/__tests__/unit/services/LabelsService.test.ts`

- [ ] **Step 1: Write the failing tests**

```typescript
// backend/__tests__/unit/services/LabelsService.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import { DatabaseConnection } from '../../../database/DatabaseConnection'
import { LabelsRepository } from '../../../repositories/LabelsRepository'
import { LabelsService } from '../../../services/LabelsService'

let conn: DatabaseConnection
let service: LabelsService

beforeEach(() => {
  conn = new DatabaseConnection(':memory:')
  service = new LabelsService(new LabelsRepository(conn.db))
})

afterEach(() => conn.close())

describe('LabelsService', () => {
  it('getLabels returns labels for a valid locale', () => {
    const labels = service.getLabels('en')
    expect(labels['games.page.title']).toBe('Games')
  })

  it('getLabels defaults to en for unknown locale', () => {
    const labels = service.getLabels('de')
    expect(Object.keys(labels).length).toBe(0)
  })

  it('getLocales returns available locales list', () => {
    const locales = service.getLocales()
    expect(locales.length).toBeGreaterThanOrEqual(2)
    expect(locales.find(l => l.locale === 'en')).toBeDefined()
    expect(locales.find(l => l.locale === 'fr')).toBeDefined()
  })
})
```

- [ ] **Step 2: Run to verify failure**

```bash
cd backend && npm run test:run -- __tests__/unit/services/LabelsService.test.ts
```

Expected: `Cannot find module '../../../services/LabelsService'`

- [ ] **Step 3: Implement LabelsService**

```typescript
// backend/services/LabelsService.ts
import type { LabelsRepository, LocaleInfo } from '../repositories/LabelsRepository'

export class LabelsService {
  constructor(private labelsRepo: LabelsRepository) {}

  getLabels(locale: string): Record<string, string> {
    return this.labelsRepo.getByLocale(locale)
  }

  getLocales(): LocaleInfo[] {
    return this.labelsRepo.getAvailableLocales()
  }
}
```

- [ ] **Step 4: Run to verify tests pass**

```bash
cd backend && npm run test:run -- __tests__/unit/services/LabelsService.test.ts
```

Expected: 3 passing

- [ ] **Step 5: Commit**

```bash
git add backend/services/LabelsService.ts backend/__tests__/unit/services/LabelsService.test.ts
git commit -m "feat(backend): LabelsService — getLabels + getLocales"
```

---

## Task 4: LabelsRoute + server wiring

**Files:**
- Create: `backend/routes/labels.ts`
- Create: `backend/__tests__/routes/labels.routes.test.ts`
- Modify: `backend/__tests__/helpers/buildTestApp.ts`
- Modify: `backend/server.ts`

- [ ] **Step 1: Write the failing route tests**

```typescript
// backend/__tests__/routes/labels.routes.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import request from 'supertest'
import { buildTestApp } from '../helpers/buildTestApp'
import type { DatabaseConnection } from '../../database/DatabaseConnection'
import type { Application } from 'express'

let app: Application
let conn: DatabaseConnection

beforeEach(() => {
  const built = buildTestApp()
  app = built.app
  conn = built.conn
})
afterEach(() => conn.close())

describe('GET /api/v1/labels', () => {
  it('retourne les labels EN sans auth', async () => {
    const res = await request(app).get('/api/v1/labels?locale=en')
    expect(res.status).toBe(200)
    expect(res.body['games.page.title']).toBe('Games')
    expect(res.body['common.buttons.save']).toBe('Save')
  })

  it('retourne les labels FR', async () => {
    const res = await request(app).get('/api/v1/labels?locale=fr')
    expect(res.status).toBe(200)
    expect(res.body['games.page.title']).toBe('Jeux')
    expect(res.body['common.buttons.save']).toBe('Enregistrer')
  })

  it('locale absente → locale en par défaut', async () => {
    const res = await request(app).get('/api/v1/labels')
    expect(res.status).toBe(200)
    expect(res.body['games.page.title']).toBe('Games')
  })

  it('locale invalide (trop courte) → 400', async () => {
    const res = await request(app).get('/api/v1/labels?locale=x')
    expect(res.status).toBe(400)
  })
})

describe('GET /api/v1/labels/locales', () => {
  it('retourne la liste des locales sans auth', async () => {
    const res = await request(app).get('/api/v1/labels/locales')
    expect(res.status).toBe(200)
    expect(Array.isArray(res.body)).toBe(true)
    const codes = res.body.map((l: { locale: string }) => l.locale)
    expect(codes).toContain('en')
    expect(codes).toContain('fr')
  })

  it('chaque locale a un champ name', async () => {
    const res = await request(app).get('/api/v1/labels/locales')
    expect(res.body[0]).toHaveProperty('locale')
    expect(res.body[0]).toHaveProperty('name')
  })
})
```

- [ ] **Step 2: Run to verify failure**

```bash
cd backend && npm run test:run -- __tests__/routes/labels.routes.test.ts
```

Expected: failing (route not registered)

- [ ] **Step 3: Implement the route**

```typescript
// backend/routes/labels.ts
import { Router } from 'express'
import { z } from 'zod'
import type { LabelsService } from '../services/LabelsService'
import { validateQuery } from '../validation/middleware'

const LocaleQuery = z.object({
  locale: z.string().min(2).max(5).default('en'),
})

export function createLabelsRouter(labelsService: LabelsService): Router {
  const router = Router()

  router.get('/locales', (_req, res) => {
    res.json(labelsService.getLocales())
  })

  router.get('/', validateQuery(LocaleQuery), (req, res) => {
    const { locale } = req.query as { locale: string }
    res.json(labelsService.getLabels(locale))
  })

  return router
}
```

> Note: `validateQuery` follows the same pattern as `validateBody` — check `backend/validation/middleware.ts` to confirm the export name. If it doesn't exist yet, add it (see step 3b below).

- [ ] **Step 3b: Check if `validateQuery` exists in validation middleware**

```bash
cd backend && grep -n "validateQuery" validation/middleware.ts
```

If the grep returns nothing, add `validateQuery` to `backend/validation/middleware.ts`:

```typescript
// Add after the existing validateBody export:
export function validateQuery<T>(schema: z.ZodSchema<T>) {
  return (req: Request, res: Response, next: NextFunction) => {
    const result = schema.safeParse(req.query)
    if (!result.success) {
      res.status(400).json({ error: 'Invalid query parameters', details: result.error.flatten() })
      return
    }
    req.query = result.data as any
    next()
  }
}
```

- [ ] **Step 4: Wire into buildTestApp**

In `backend/__tests__/helpers/buildTestApp.ts`, add the imports and wiring:

```typescript
// Add to existing imports:
import { LabelsRepository } from '../../repositories/LabelsRepository'
import { LabelsService } from '../../services/LabelsService'
import { createLabelsRouter } from '../../routes/labels'
```

In the `buildTestApp` function body, after `const bggRepo`:
```typescript
const labelsRepo = new LabelsRepository(conn.db)
const labelsService = new LabelsService(labelsRepo)
```

After `app.use('/api/v1/bgg', ...)`:
```typescript
app.use('/api/v1/labels', createLabelsRouter(labelsService))
```

- [ ] **Step 5: Run route tests to verify they pass**

```bash
cd backend && npm run test:run -- __tests__/routes/labels.routes.test.ts
```

Expected: 6 passing

- [ ] **Step 6: Wire into server.ts**

In `backend/server.ts`, add the imports:

```typescript
import { LabelsRepository } from './repositories/LabelsRepository'
import { LabelsService } from './services/LabelsService'
import { createLabelsRouter } from './routes/labels'
```

After the existing repository/service instantiations (after `const bggRepo`):

```typescript
const labelsRepo    = new LabelsRepository(dbConn.db)
const labelsService = new LabelsService(labelsRepo)
```

Register the route **before** `authenticate` routes (labels are public — no auth):

```typescript
// Add before the authenticated routes block:
app.use('/api/v1/labels', createLabelsRouter(labelsService))
```

- [ ] **Step 7: Run full backend test suite**

```bash
cd backend && npm run test:run
```

Expected: all tests pass (no regressions)

- [ ] **Step 8: Commit**

```bash
git add backend/routes/labels.ts backend/__tests__/routes/labels.routes.test.ts backend/__tests__/helpers/buildTestApp.ts backend/server.ts backend/validation/middleware.ts
git commit -m "feat(backend): labels routes — GET /labels?locale + GET /labels/locales (public)"
```

---

## Task 5: Static EN fallback JSON

**Files:**
- Create: `src/i18n/en.json`

- [ ] **Step 1: Create the static EN bundle**

Extract all `en` values from the migration seed into a JSON file. This file must stay in sync with the `en` rows in the migration — it's the offline fallback only.

```json
{
  "locale.en.name": "English",
  "locale.fr.name": "Français",
  "common.buttons.save": "Save",
  "common.buttons.cancel": "Cancel",
  "common.buttons.delete": "Delete",
  "common.buttons.edit": "Edit",
  "common.buttons.add": "Add",
  "common.buttons.confirm": "Confirm",
  "common.buttons.back": "Back",
  "common.buttons.retry": "Retry",
  "common.buttons.submit": "Submit",
  "common.buttons.create": "Create",
  "common.buttons.close": "Close",
  "common.buttons.loading": "Loading...",
  "common.search.placeholder": "Search...",
  "common.empty.no_results": "No results found",
  "common.action.cannot_undo": "This action cannot be undone.",
  "auth.title": "Board Game Dashboard",
  "auth.subtitle": "Enter your password to continue",
  "auth.password.label": "Password",
  "auth.submit": "Sign in",
  "auth.submit.loading": "Signing in...",
  "auth.error.wrong_password": "Incorrect password. Please try again.",
  "dashboard.title": "Dashboard",
  "dashboard.tooltip.back": "Go Back",
  "dashboard.tooltip.settings": "Settings",
  "dashboard.section.players": "Player Statistics",
  "dashboard.section.games": "Recent Games",
  "dashboard.section.activity": "Recent Activity",
  "dashboard.players.empty": "No players yet",
  "dashboard.players.add_first": "Add your first player",
  "dashboard.games.empty": "No games yet",
  "dashboard.games.add_first": "Add your first game",
  "dashboard.activity.game_started": "New game started",
  "dashboard.activity.player_joined": "Player joined",
  "dashboard.actions.new_game": "New Game",
  "dashboard.actions.new_game.tooltip": "Start a New Game Session",
  "dashboard.actions.add_player": "Add Player",
  "dashboard.actions.add_player.tooltip": "Go to Players Page",
  "dashboard.tooltip.view_players": "View All Players",
  "dashboard.tooltip.view_games": "View All Games",
  "games.page.title": "Games",
  "games.stats.total": "Total Games",
  "games.stats.categories": "Categories",
  "games.stats.avg_rating": "Avg Rating",
  "games.search.placeholder": "Search games, designers, publishers...",
  "games.card.players": "players",
  "games.card.weight": "Weight:",
  "games.card.expansion": "Extension",
  "games.card.expansion.count": "expansion",
  "games.card.expansion.count_plural": "expansions",
  "games.card.character.count": "character",
  "games.card.character.count_plural": "characters",
  "games.card.section.expansions": "Expansions",
  "games.card.section.characters": "Characters/Roles",
  "games.card.modes.competitive": "Compétitif",
  "games.card.modes.cooperative": "Coopératif",
  "games.card.modes.campaign": "Campagne",
  "games.card.modes.hybrid": "Hybride",
  "games.empty": "No games found",
  "games.tooltip.view_details": "View Details",
  "games.tooltip.view_stats": "View Game Stats",
  "games.tooltip.edit": "Edit Game",
  "games.menu.view_details": "View Details",
  "games.menu.view_stats": "View Stats",
  "games.menu.edit": "Edit Game",
  "games.menu.expansions": "Manage Expansions",
  "games.menu.characters": "Manage Characters",
  "games.menu.delete": "Delete Game",
  "games.add_dialog.title": "Add New Game",
  "games.add_dialog.submit": "Add Game",
  "games.edit_dialog.title": "Edit Game",
  "games.edit_dialog.submit": "Update Game",
  "games.delete_dialog.title": "Delete Game",
  "games.delete_dialog.description": "Are you sure you want to delete this game? This action cannot be undone and will also remove all associated expansions and characters.",
  "games.form.name.label": "Game Name",
  "games.form.name.placeholder": "Enter game name",
  "games.form.designer.label": "Designer",
  "games.form.publisher.label": "Publisher",
  "games.form.year.label": "Year",
  "games.form.bgg_rating.label": "BGG Rating",
  "games.form.description.label": "Description",
  "games.form.description.placeholder": "Brief game description",
  "games.form.category.label": "Category",
  "games.form.modes.label": "Game Modes",
  "games.form.is_expansion.label": "Is an expansion",
  "games.form.has_expansion.label": "Has expansions",
  "games.form.expansions.label": "Expansions",
  "games.form.has_characters.label": "Has character roles",
  "games.form.characters.section": "Characters",
  "games.form.characters.name.placeholder": "Character name",
  "games.form.characters.abilities.label": "Abilities",
  "games.form.characters.ability.placeholder": "Ability name",
  "players.page.title": "Players",
  "players.stats.total": "Players",
  "players.stats.games": "Games",
  "players.stats.wins": "Wins",
  "players.search.placeholder": "Search players...",
  "players.card.games": "games",
  "players.card.wins": "wins",
  "players.card.avg": "Avg:",
  "players.card.menu.view_stats": "View Stats",
  "players.card.menu.edit": "Edit",
  "players.card.menu.delete": "Delete",
  "players.empty": "No players found",
  "players.empty.add_first": "Add Your First Player",
  "players.add_dialog.title": "Add New Player",
  "players.edit_dialog.title": "Edit Player",
  "players.delete_dialog.title": "Delete Player",
  "players.delete_dialog.description": "Are you sure you want to delete this player? This action cannot be undone.",
  "players.form.name.label": "Name",
  "players.form.avatar.label": "Avatar URL",
  "players.form.favorite_game.label": "Favorite Game",
  "players.form.favorite_game.placeholder": "Enter favorite game (optional)",
  "sessions.new.title": "New Game Session",
  "sessions.setup.title": "Game Setup",
  "sessions.setup.game.label": "Game",
  "sessions.setup.game.placeholder": "Choose a game...",
  "sessions.setup.type.label": "Session Type",
  "sessions.setup.type.competitive": "Competitive - Players compete against each other",
  "sessions.setup.type.cooperative": "Cooperative - Players work together",
  "sessions.setup.type.campaign": "Campaign - Ongoing story mode",
  "sessions.setup.type.hybrid": "Hybrid - Mix of team and individual goals",
  "sessions.players.title": "Select Players",
  "sessions.cooperative.title": "Cooperative Objectives",
  "sessions.cooperative.difficulty.label": "Difficulty Level",
  "sessions.cooperative.team_success": "Team Victory",
  "sessions.cooperative.team_score.label": "Team Score",
  "sessions.cooperative.objectives.label": "Shared Objectives",
  "sessions.cooperative.objectives.add_common": "Add Common",
  "sessions.cooperative.objectives.add_custom": "Add Custom",
  "sessions.cooperative.objectives.empty": "No objectives set yet",
  "sessions.cooperative.objectives.completed": "Completed",
  "sessions.competitive.title": "Competitive Scoring",
  "sessions.competitive.winner_missing": "A winner must be designated",
  "sessions.competitive.score_invalid": "The winner's score must be greater than 0",
  "sessions.competitive.winner": "Winner",
  "sessions.hybrid.team.title": "Team Objectives",
  "sessions.hybrid.team.completed": "Team Objectives Completed",
  "sessions.hybrid.team.score": "Team Score",
  "sessions.hybrid.individual.title": "Individual Scores",
  "sessions.details.title": "Session Details",
  "sessions.details.duration.label": "Duration (minutes)",
  "sessions.details.duration.required": "Required",
  "sessions.details.notes.label": "Notes",
  "sessions.details.notes.placeholder": "Game notes, highlights, or observations...",
  "sessions.submit": "Create Session",
  "sessions.submit.loading": "Creating...",
  "stats.game.no_data": "No game data available",
  "stats.game.all_title": "All Games Statistics",
  "stats.game.stat.sessions": "Sessions",
  "stats.game.stat.avg_players": "Avg Players",
  "stats.game.stat.avg_duration": "Avg Duration",
  "stats.game.stat.avg_score": "Avg Score",
  "stats.game.back_to_all": "← Back to All Games Statistics",
  "stats.game.select.title": "Select a Game for Detailed Stats",
  "stats.game.popularity.title": "Game Popularity",
  "stats.game.sessions_label": "sessions",
  "stats.game.score_trend.title": "Score Trend",
  "stats.game.score_trend.subtitle": "Last 10 Sessions",
  "stats.game.session_types.title": "Session Types",
  "stats.game.player_dist.title": "Player Count Distribution",
  "stats.game.player_dist.label": "players",
  "stats.game.top_winners.title": "Top Winners",
  "stats.game.top_winners.wins": "wins",
  "stats.game.recent.title": "Recent Sessions",
  "stats.game.recent.winner": "Winner:",
  "stats.game.recent.avg_score": "avg score",
  "stats.player.profile": "Player Profile",
  "stats.player.stat.wins": "Wins",
  "stats.player.stat.games_played": "Games Played",
  "stats.player.stat.total_score": "Total Score",
  "stats.player.stat.avg_score": "Avg Score",
  "stats.player.stat.total_players": "Total Players",
  "stats.player.favorite_game": "Favorite Game",
  "stats.player.top_players": "Top Players",
  "stats.player.recent.empty": "No recent games found",
  "stats.player.recent.winner": "Winner",
  "stats.player.activity.title": "Recent Activity",
  "stats.player.performance.coming_soon": "Detailed charts coming soon...",
  "settings.page.title": "Settings",
  "settings.section.preferences": "Preferences",
  "settings.notifications.label": "Notifications",
  "settings.notifications.desc": "Get notified about game updates",
  "settings.dark_mode.label": "Dark Mode",
  "settings.dark_mode.desc": "Use dark theme",
  "settings.language.label": "Language",
  "settings.language.desc": "Choose your language",
  "settings.language.offline": "Unavailable offline",
  "settings.language.retry": "Retry",
  "settings.auto_save.label": "Auto Save",
  "settings.auto_save.desc": "Automatically save changes",
  "settings.tooltips.label": "Show Tooltips",
  "settings.tooltips.desc": "Display helpful tooltips",
  "settings.section.data": "Data Management",
  "settings.data.bgg_imported": "BGG Catalog imported",
  "settings.data.export": "Export Data",
  "settings.data.import": "Import Data",
  "settings.data.reset": "Reset All Data",
  "settings.data.bgg_catalog": "BGG Catalog",
  "settings.data.bgg_not_imported": "Not imported",
  "settings.data.bgg_importing": "Import in progress…",
  "settings.data.bgg_import_file": "Import boardgames_ranks.csv",
  "settings.section.about": "About",
  "settings.about.version": "Board Game Dashboard v1.0.0",
  "settings.about.desc": "A modern dashboard for tracking your board game sessions and player statistics.",
  "settings.section.session": "Session",
  "settings.logout": "Sign out",
  "errors.generic": "An error occurred",
  "errors.network": "Network error. Please check your connection.",
  "errors.game.not_found": "Game not found",
  "errors.game.duplicate": "This game is already in your collection",
  "errors.player.not_found": "Player not found",
  "errors.player.duplicate_pseudo": "This pseudo is already taken",
  "errors.session.not_found": "Session not found",
  "errors.auth.unauthorized": "Unauthorized",
  "errors.auth.wrong_password": "Incorrect password"
}
```

- [ ] **Step 2: Commit**

```bash
git add src/i18n/en.json
git commit -m "feat(frontend): static EN fallback bundle (src/i18n/en.json)"
```

---

## Task 6: labelsApi.ts

**Files:**
- Create: `src/services/api/labelsApi.ts`

- [ ] **Step 1: Create the API service**

```typescript
// src/services/api/labelsApi.ts
export interface LocaleInfo {
  locale: string
  name: string
}

async function request<T>(url: string): Promise<T> {
  const res = await fetch(url, { credentials: 'include' })
  if (!res.ok) throw new Error(`HTTP ${res.status}`)
  return res.json() as Promise<T>
}

export const labelsApi = {
  fetchLabels: (locale: string): Promise<Record<string, string>> =>
    request<Record<string, string>>(`/api/v1/labels?locale=${encodeURIComponent(locale)}`),

  fetchLocales: (): Promise<LocaleInfo[]> =>
    request<LocaleInfo[]>('/api/v1/labels/locales'),
}
```

- [ ] **Step 2: Commit**

```bash
git add src/services/api/labelsApi.ts
git commit -m "feat(frontend): labelsApi — fetchLabels + fetchLocales"
```

---

## Task 7: useApiReachable

**Files:**
- Create: `src/hooks/useApiReachable.ts`
- Create: `src/__tests__/hooks/useApiReachable.test.ts`

- [ ] **Step 1: Write the failing tests**

```typescript
// src/__tests__/hooks/useApiReachable.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook, act, waitFor } from '@testing-library/react'
import { useApiReachable } from '@/hooks/useApiReachable'

global.fetch = vi.fn()

beforeEach(() => vi.clearAllMocks())

describe('useApiReachable', () => {
  it('isReachable is true when /api/health returns 200', async () => {
    vi.mocked(fetch).mockResolvedValueOnce({ ok: true } as Response)
    const { result } = renderHook(() => useApiReachable())
    await waitFor(() => expect(result.current.isReachable).toBe(true))
  })

  it('isReachable is false when fetch throws', async () => {
    vi.mocked(fetch).mockRejectedValueOnce(new Error('Network error'))
    const { result } = renderHook(() => useApiReachable())
    await waitFor(() => expect(result.current.isReachable).toBe(false))
  })

  it('isReachable is false when /api/health returns non-200', async () => {
    vi.mocked(fetch).mockResolvedValueOnce({ ok: false } as Response)
    const { result } = renderHook(() => useApiReachable())
    await waitFor(() => expect(result.current.isReachable).toBe(false))
  })

  it('triggerRetry re-checks reachability', async () => {
    vi.mocked(fetch)
      .mockRejectedValueOnce(new Error('down'))
      .mockResolvedValueOnce({ ok: true } as Response)
    const { result } = renderHook(() => useApiReachable())
    await waitFor(() => expect(result.current.isReachable).toBe(false))
    act(() => result.current.triggerRetry())
    await waitFor(() => expect(result.current.isReachable).toBe(true))
  })
})
```

- [ ] **Step 2: Run to verify failure**

```bash
npm run test:run -- src/__tests__/hooks/useApiReachable.test.ts
```

Expected: `Cannot find module '@/hooks/useApiReachable'`

- [ ] **Step 3: Implement useApiReachable**

```typescript
// src/hooks/useApiReachable.ts
import { useState, useEffect, useCallback } from 'react'

export function useApiReachable() {
  const [isReachable, setIsReachable] = useState<boolean>(true)
  const [retryTick, setRetryTick] = useState(0)

  useEffect(() => {
    let cancelled = false
    fetch('/api/health', { credentials: 'include' })
      .then(res => { if (!cancelled) setIsReachable(res.ok) })
      .catch(() => { if (!cancelled) setIsReachable(false) })
    return () => { cancelled = true }
  }, [retryTick])

  const triggerRetry = useCallback(() => setRetryTick(t => t + 1), [])

  return { isReachable, triggerRetry }
}
```

- [ ] **Step 4: Run to verify tests pass**

```bash
npm run test:run -- src/__tests__/hooks/useApiReachable.test.ts
```

Expected: 4 passing

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useApiReachable.ts src/__tests__/hooks/useApiReachable.test.ts
git commit -m "feat(frontend): useApiReachable — ping /api/health + triggerRetry"
```

---

## Task 8: useLocale

**Files:**
- Create: `src/hooks/useLocale.ts`

- [ ] **Step 1: Implement useLocale**

```typescript
// src/hooks/useLocale.ts
import { useCallback } from 'react'
import { useQueryClient } from '@tanstack/react-query'

const STORAGE_KEY = 'locale'
const DEFAULT_LOCALE = 'en'

export function getStoredLocale(): string {
  return localStorage.getItem(STORAGE_KEY) ?? DEFAULT_LOCALE
}

export function useLocale(): [string, (locale: string) => void] {
  const queryClient = useQueryClient()
  const locale = getStoredLocale()

  const setLocale = useCallback((newLocale: string) => {
    localStorage.setItem(STORAGE_KEY, newLocale)
    queryClient.invalidateQueries({ queryKey: ['labels'] })
  }, [queryClient])

  return [locale, setLocale]
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useLocale.ts
git commit -m "feat(frontend): useLocale — localStorage persistence + React Query invalidation"
```

---

## Task 9: useLabels

**Files:**
- Create: `src/hooks/useLabels.ts`
- Create: `src/__tests__/hooks/useLabels.test.ts`

- [ ] **Step 1: Write the failing tests**

```typescript
// src/__tests__/hooks/useLabels.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook, waitFor } from '@testing-library/react'
import { useLabels } from '@/hooks/useLabels'
import { createHookWrapper } from '@/__tests__/utils/test-utils'

vi.mock('@/services/api/labelsApi', () => ({
  labelsApi: {
    fetchLabels: vi.fn(),
    fetchLocales: vi.fn(),
  },
}))

vi.mock('@/hooks/useLocale', () => ({
  useLocale: () => ['en', vi.fn()],
  getStoredLocale: () => 'en',
}))

const wrapper = createHookWrapper('/')

describe('useLabels', () => {
  beforeEach(() => vi.clearAllMocks())

  it('t() returns the value from the API response', async () => {
    const { labelsApi } = await import('@/services/api/labelsApi')
    vi.mocked(labelsApi.fetchLabels).mockResolvedValue({
      'games.page.title': 'Games',
      'common.buttons.save': 'Save',
    })
    const { result } = renderHook(() => useLabels(), { wrapper })
    await waitFor(() => expect(result.current.isLoading).toBe(false))
    expect(result.current.t('games.page.title')).toBe('Games')
  })

  it('t() returns fallback string when key is missing', async () => {
    const { labelsApi } = await import('@/services/api/labelsApi')
    vi.mocked(labelsApi.fetchLabels).mockResolvedValue({})
    const { result } = renderHook(() => useLabels(), { wrapper })
    await waitFor(() => expect(result.current.isLoading).toBe(false))
    expect(result.current.t('missing.key', 'Default')).toBe('Default')
  })

  it('t() returns key name when key missing and no fallback provided', async () => {
    const { labelsApi } = await import('@/services/api/labelsApi')
    vi.mocked(labelsApi.fetchLabels).mockResolvedValue({})
    const { result } = renderHook(() => useLabels(), { wrapper })
    await waitFor(() => expect(result.current.isLoading).toBe(false))
    expect(result.current.t('missing.key')).toBe('missing.key')
  })

  it('falls back to en.json when API call fails', async () => {
    const { labelsApi } = await import('@/services/api/labelsApi')
    vi.mocked(labelsApi.fetchLabels).mockRejectedValue(new Error('Network error'))
    const { result } = renderHook(() => useLabels(), { wrapper })
    await waitFor(() => expect(result.current.isLoading).toBe(false))
    // en.json has games.page.title = "Games"
    expect(result.current.t('games.page.title')).toBe('Games')
  })
})
```

- [ ] **Step 2: Run to verify failure**

```bash
npm run test:run -- src/__tests__/hooks/useLabels.test.ts
```

Expected: `Cannot find module '@/hooks/useLabels'`

- [ ] **Step 3: Implement useLabels**

```typescript
// src/hooks/useLabels.ts
import { useQuery } from '@tanstack/react-query'
import { labelsApi } from '@/services/api/labelsApi'
import { getStoredLocale } from '@/hooks/useLocale'
import enFallback from '@/i18n/en.json'

export function useLabels() {
  const locale = getStoredLocale()

  const { data, isLoading } = useQuery<Record<string, string>>({
    queryKey: ['labels', locale],
    queryFn: () => labelsApi.fetchLabels(locale),
    staleTime: Infinity,
    placeholderData: enFallback as Record<string, string>,
  })

  const labels = data ?? (enFallback as Record<string, string>)

  function t(key: string, fallback?: string): string {
    return labels[key] ?? fallback ?? key
  }

  return { t, isLoading }
}
```

- [ ] **Step 4: Run to verify tests pass**

```bash
npm run test:run -- src/__tests__/hooks/useLabels.test.ts
```

Expected: 4 passing

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useLabels.ts src/__tests__/hooks/useLabels.test.ts
git commit -m "feat(frontend): useLabels — t(key, fallback) with EN JSON offline fallback"
```

---

## Task 10: useLocales

**Files:**
- Create: `src/hooks/useLocales.ts`
- Create: `src/__tests__/hooks/useLocales.test.ts`

- [ ] **Step 1: Write the failing tests**

```typescript
// src/__tests__/hooks/useLocales.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook, waitFor } from '@testing-library/react'
import { useLocales } from '@/hooks/useLocales'
import { createHookWrapper } from '@/__tests__/utils/test-utils'

vi.mock('@/services/api/labelsApi', () => ({
  labelsApi: {
    fetchLabels: vi.fn(),
    fetchLocales: vi.fn(),
  },
}))

const wrapper = createHookWrapper('/')

describe('useLocales', () => {
  beforeEach(() => vi.clearAllMocks())

  it('returns locale list from API when reachable', async () => {
    const { labelsApi } = await import('@/services/api/labelsApi')
    vi.mocked(labelsApi.fetchLocales).mockResolvedValue([
      { locale: 'en', name: 'English' },
      { locale: 'fr', name: 'Français' },
    ])
    const { result } = renderHook(() => useLocales(true), { wrapper })
    await waitFor(() => expect(result.current.isLoading).toBe(false))
    expect(result.current.locales).toHaveLength(2)
    expect(result.current.locales[0].locale).toBe('en')
  })

  it('returns empty array and does not fetch when not reachable', async () => {
    const { labelsApi } = await import('@/services/api/labelsApi')
    vi.mocked(labelsApi.fetchLocales).mockResolvedValue([])
    const { result } = renderHook(() => useLocales(false), { wrapper })
    expect(result.current.locales).toEqual([])
    expect(labelsApi.fetchLocales).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: Run to verify failure**

```bash
npm run test:run -- src/__tests__/hooks/useLocales.test.ts
```

Expected: `Cannot find module '@/hooks/useLocales'`

- [ ] **Step 3: Implement useLocales**

```typescript
// src/hooks/useLocales.ts
import { useQuery } from '@tanstack/react-query'
import { labelsApi } from '@/services/api/labelsApi'
import type { LocaleInfo } from '@/services/api/labelsApi'

export function useLocales(isReachable: boolean) {
  const { data: locales = [], isLoading } = useQuery<LocaleInfo[]>({
    queryKey: ['labels', 'locales'],
    queryFn: () => labelsApi.fetchLocales(),
    staleTime: Infinity,
    enabled: isReachable,
  })

  return { locales, isLoading }
}
```

- [ ] **Step 4: Run to verify tests pass**

```bash
npm run test:run -- src/__tests__/hooks/useLocales.test.ts
```

Expected: 2 passing

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useLocales.ts src/__tests__/hooks/useLocales.test.ts
git commit -m "feat(frontend): useLocales — dynamic locale list, skipped when offline"
```

---

## Task 11: Settings — wire locale + dynamic select + offline UX

**Files:**
- Modify: `src/hooks/useSettingsPage.ts`
- Modify: `src/views/SettingsPageView.tsx`

- [ ] **Step 1: Update useSettingsPage to use useLocale**

Replace the `language` stub in `src/hooks/useSettingsPage.ts`:

```typescript
// src/hooks/useSettingsPage.ts
// Add import at top:
import { useLocale } from '@/hooks/useLocale'
import { useApiReachable } from '@/hooks/useApiReachable'
import { useLocales } from '@/hooks/useLocales'

// Inside useSettingsPage():
// Remove: const [language, setLanguage] = useState('en');
// Add:
const [locale, setLocale] = useLocale()
const { isReachable, triggerRetry } = useApiReachable()
const { locales } = useLocales(isReachable)
```

Update the returned object — remove `language` and `handleLanguageChange`, add:

```typescript
// In the return object, replace:
//   language,
//   handleLanguageChange: setLanguage,
// With:
locale,
locales,
isApiReachable: isReachable,
handleLanguageChange: setLocale,
handleRetryConnection: triggerRetry,
```

- [ ] **Step 2: Update SettingsPageView props interface**

In `src/views/SettingsPageView.tsx`, update the `SettingsPageViewProps` interface:

```typescript
// Replace:
//   language: string;
// With:
locale: string;
locales: Array<{ locale: string; name: string }>;
isApiReachable: boolean;
handleRetryConnection: () => void;
// Keep: handleLanguageChange: (lang: string) => void;
```

- [ ] **Step 3: Update the language selector in SettingsPageView**

Replace the hardcoded `<Select>` block (lines ~127-136) with the dynamic version:

```tsx
<div className="flex items-center justify-between">
  <div className="flex items-center space-x-3">
    <Globe className="w-5 h-5 text-green-400" />
    <div>
      <div className={labelClass}>Language</div>
      <div className={descClass}>Choose your language</div>
    </div>
  </div>
  <div className="flex items-center gap-2">
    {!props.isApiReachable && (
      <button
        onClick={props.handleRetryConnection}
        className="text-xs text-orange-400 hover:text-orange-300 transition-colors underline"
      >
        Retry
      </button>
    )}
    <Select
      value={props.locale}
      onValueChange={props.handleLanguageChange}
      disabled={props.locales.length === 0}
    >
      <SelectTrigger className="w-32">
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        {props.locales.map(l => (
          <SelectItem
            key={l.locale}
            value={l.locale}
            disabled={l.locale !== 'en' && !props.isApiReachable}
          >
            {l.locale !== 'en' && !props.isApiReachable
              ? `${l.name} (offline)`
              : l.name}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  </div>
</div>
```

- [ ] **Step 4: Remove ES from the hardcoded select (it no longer exists)**

The ES option was hardcoded in the old `<SelectItem value="es">Español</SelectItem>` — it's now gone since the select is driven by `props.locales`. No separate action needed.

- [ ] **Step 5: Run full frontend test suite**

```bash
npm run test:run
```

Expected: all existing tests still pass. If `useSettingsPage` has tests, update their mock to reflect the renamed props (`locale` instead of `language`, etc.).

- [ ] **Step 6: Commit**

```bash
git add src/hooks/useSettingsPage.ts src/views/SettingsPageView.tsx
git commit -m "feat(frontend): Settings — dynamic locale select, offline grey-out + retry"
```

---

## Task 12: ROADMAP + final verification

**Files:**
- Modify: `ROADMAP.md`

- [ ] **Step 1: Run complete test suites**

```bash
npm run test:run
cd backend && npm run test:run
```

Expected: all tests pass in both frontend and backend.

- [ ] **Step 2: Update ROADMAP**

In `ROADMAP.md`, mark the labels i18n item in Sprint 5 as delivered:

```markdown
1. ~~**🌍 Internationalisation DB-driven**~~ — ✅ **Livré (PR #XX)** — Table `labels`, endpoints `/api/v1/labels` + `/api/v1/labels/locales`, hook `useLabels`, sélecteur dynamique dans Settings.
```

Also in the "Localisation des labels — DÉFÉRÉ" section, note that the infrastructure is now in place and the remaining work is wiring `t()` calls into each view/dialog component.

- [ ] **Step 3: Final commit**

```bash
git add ROADMAP.md
git commit -m "docs: mark labels i18n infrastructure as delivered in ROADMAP"
```

---

## Post-plan notes

### `validateQuery` — if it doesn't exist yet

The route uses `validateQuery(LocaleQuery)`. Check `backend/validation/middleware.ts` — if only `validateBody` and `validateParams` exist, add `validateQuery` in Task 4 step 3b. The pattern is identical to `validateBody` but reads from `req.query`.

### Wiring `t()` into views — next phase

This plan delivers the **infrastructure**: DB, API, hooks. Replacing the hardcoded strings in each view (`DashboardView`, `GamesPageView`, `PlayersPageView`, `NewGameView`, `GameStatsView`, `PlayerStatsView`, `SettingsPageView`, dialogs, `LoginPage`) is a **follow-up task** — each view gets `const { t } = useLabels()` and strings become `t('key')`. That's mechanical work, best done view-by-view in separate commits.
